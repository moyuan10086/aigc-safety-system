# 昇腾 910C 真实模型推理验证报告

日期：2026-08-04

## 结论

在不停止、不抢占现有任务的前提下，复用 `100.121.203.95` 上已运行的 vLLM-Ascend 服务，完成一次 Qwen3-235B-A22B-Thinking-2507 W8A8 真实推理。请求 HTTP 200，延迟 2682 ms，输入 57 tokens，输出 64 tokens，总计 121 tokens。

这证明现有国产昇腾链路能够执行真实大模型推理，但不等于已经部署 Qwen3Guard。该模型在本次“只输出 JSON”的提示下仍产生解释文本，因此若接入安全审核主系统，必须在平台侧增加结构化解析、规则复核、失败降级和输出审计，不能直接把生成文本当作最终安全判定。

## 资源与安全边界

- 16 张 Ascend910C 均为健康状态。
- 物理卡 4–7 已被 8 个 `VLLMWorker_TP` 进程占用，每卡约 57 GB HBM；本次只调用已有 HTTP 服务，没有启动新模型进程。
- 物理卡 0 存在长期 Python 任务；未停止、未迁移、未抢占。
- 根盘 445 GB，已使用 94%，剩余约 31 GB；未下载新权重，未删除或清理任何用户文件。
- `/mnt/model` 为 dtfs 模型存储，500 TB，总使用约 53%；只读使用已有模型。
- 内部推理端点仅监听服务器环境，不作为比赛对外 API。未来如接入主系统，应由受控护栏网关代理。

## 实际输入输出摘要

输入为普通产品说明的安全分类请求，不包含隐私、凭证或危险动作。模型响应 HTTP 200，输出 SHA-256 为 `ccf375d240e2cc99b3dda75a18e48d35bef4a83062cee7997babc5e7e398a811`。报告只保留必要预览与哈希，不记录内部服务凭据。

## 后续实施计划

1. 不直接把 Qwen3-235B 生成结果当作护栏标签；增加 `safe/borderline/unsafe` 结构化适配器和 schema 校验。
2. 在平台服务端通过配置连接内部 NPU endpoint，继续沿用超时、一次有界重试、脱敏、审计和规则/RAG 降级。
3. 用公开的安全、边界、危险中文样本做三类验证，记录准确率、拒答、延迟和解析失败率。
4. 专用 Qwen3Guard 权重可用且卡位安全时再做分类器对比；不在 94% 根盘下载大权重，不抢占物理卡 0 或现有 4–7 服务。

结构化证据见 `docs/evidence/ascend-910c-real-inference-20260804.json`。
